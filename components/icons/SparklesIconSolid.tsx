import React from 'react';

const SparklesIconSolid: React.FC<{ className?: string }> = ({ className }) => {
    // This icon is no longer used. Returning null to avoid rendering errors.
    return null; 
};

export default SparklesIconSolid;
